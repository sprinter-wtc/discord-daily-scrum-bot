name: Daily Scrum Reminder

env:
  DISCORD_MESSAGE: "@everyone 💬 데일리 스크럼 시간입니다! 아래 포맷에 맞춰 진행 상황을 공유해주세요 🙌\\n1️⃣ 어제 한 일\\n2️⃣ 오늘 할 일\\n3️⃣ 막힌 점"
  WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

on:
  schedule:
    - cron: '0 4 */2 * *'  # UTC 기준, 한국시간 13:00 격일
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Reminder
        run: |
          echo "{\"content\": \"${DISCORD_MESSAGE}\"}" | \
          curl -H "Content-Type: application/json" \
               -X POST \
               -d @- \
               ${{ env.WEBHOOK_URL }}