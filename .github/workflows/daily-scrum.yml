name: Daily Scrum Reminder

env:
  WEBHOOK_URL: ${{ secrets.DISCORD_TEST_WEBHOOK_URL }}

on:
  push:
    branches:
      - main
      - test/*
  schedule:
    - cron: '0 4 * * *'  # UTC 기준, 한국시간 13:00 매일
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Reminder
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d @- \
               ${{ env.WEBHOOK_URL }} <<EOF
{
  "content": "@everyone 💬 데일리 스크럼 시간입니다!

먼저 \"[$(date +'%Y.%m.%d')] 데일리 스크럼\" 이름으로 스레드를 생성한 뒤,
아래 포맷에 맞춰 진행 상황을 공유해주세요 🙌

1. 어제 한 일
2. 오늘 할 일
3. 막힌 점"
}
EOF